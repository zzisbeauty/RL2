model_name: ${actor.model_name}
dtype: ${actor.dtype}
tf_config:
  pipeline_model_parallel_size: ${actor.tf_config.pipeline_model_parallel_size}
  virtual_pipeline_model_parallel_size: ${actor.tf_config.virtual_pipeline_model_parallel_size}
  context_parallel_size: ${actor.tf_config.context_parallel_size}
  tensor_model_parallel_size: ${actor.tf_config.tensor_model_parallel_size}
  expert_model_parallel_size: ${actor.tf_config.expert_model_parallel_size}
  expert_tensor_parallel_size: ${actor.tf_config.expert_tensor_parallel_size}
ddp_config:
  use_distributed_optimizer: ${actor.ddp_config.use_distributed_optimizer}
max_inference_length_per_device: ${actor.max_length_per_device}
offload_model: true